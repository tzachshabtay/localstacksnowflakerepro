CREATE TABLE IF NOT EXISTS MYSCHEMA.MYTABLE (
	COL1 VARCHAR(16777216) NOT NULL,
	COL2 DATE NOT NULL,
	COL3 VARCHAR(16777216) NOT NULL,
	COL4 NUMBER(38,0) NOT NULL,
	COL5 VARCHAR(16777216),
	COL6 VARCHAR(16777216),
	COL7 TIMESTAMP_NTZ(9),
	COL8 VARCHAR(16777216),
	constraint PK_MYTABLE primary key (COL1, COL2, COL4)
);

CREATE TABLE IF NOT EXISTS MYSCHEMA.MYTABLE2 (
	RECORD_CONTENT VARIANT
);

CREATE STAGE IF NOT EXISTS MYSCHEMA.MYSTAGE;

CREATE OR REPLACE FILE FORMAT MYSCHEMA.MYFORMAT
    TYPE = 'CSV'
    SKIP_HEADER = 1
    FIELD_OPTIONALLY_ENCLOSED_BY = '"';